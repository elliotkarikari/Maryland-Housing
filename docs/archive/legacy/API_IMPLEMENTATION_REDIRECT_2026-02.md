# API Implementation (Moved)

API implementation documentation has been consolidated into:

- `docs/api/API_REFERENCE.md`

Canonical runtime entrypoint remains:

- `src/api/main.py`

Use `make serve` (or `python -m uvicorn src.api.main:app --reload`) for local API runs.
